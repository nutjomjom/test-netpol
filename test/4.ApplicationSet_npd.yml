#static cluster name and environment
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: test-npd-network-policies
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: https://github.com/nutjomjom/test-netpol.git
        revision: HEAD
        directories:
          - path: test/overlay/nut/*
  template:
    metadata:
      name: "{{path.basename}}-npd-network-policy"
    spec:
      project: nut
      source:
        repoURL: https://github.com/nutjomjom/test-netpol.git
        targetRevision: HEAD
        path: "test/overlay/nut/{{path.basename}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{path.basename}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
